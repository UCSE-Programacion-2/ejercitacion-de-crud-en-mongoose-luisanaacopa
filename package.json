{
  "name": "ejercitacion-mongoose-crud",
  "version": "1.0.0",
  "description": "Ejercitación de ExpressJS interactuando con MongoDB mediante Mongoose",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js",
    "test": "jest --detectOpenHandles"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^7.4.0",
    "mongoose": "^8.5.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mongodb-memory-server": "^10.0.0",
    "supertest": "^7.0.0"
  }
}
